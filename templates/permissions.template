<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Go OAuth 2.0 Permissions</title>
  </head>
  <style>
    body {
      font-family: Verdana, Geneva, Tahoma, sans-serif;
      font-size: 18px;
      color: #3b3c36;
      width: 30rem;
      margin: 50px auto;
    }

    em {
      font-weight: 600;
      color: #007fff;
    }

    button {
      font-weight: 600;
      font-size: 18px;
      font-family: Verdana, Geneva, Tahoma, sans-serif;
      color: white;
      background-color: #007fff;
      border: 1px solid #007fff;
      padding: 8px 16px;
      cursor: pointer;
    }

    .permissions-container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .permissions {
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      row-gap: 10px;
      background-color: #f5f5f5;
      border-radius: 4px;
      padding: 16px 8px;
      list-style-position: inside;
    }
  </style>
  <body>
    <em>{{ .Client }}</em> is asking for permission to:

    <div class="permissions-container">
      <ul class="permissions">
        {{ range $element := .Permissions }}
        <li>{{ $element }}</li>
        {{ end }}
      </ul>
      <button>Allow</button>
    </div>
  </body>
</html>
